# Shared UI components

No component framework or reusable component library is present. The site is a single static HTML page; its repeated-looking visual elements are inline page markup, not shared components. No component extraction is applicable.
