# Routes

- `/` — the static page `dist/index.html`, served from `dist/` as configured in `.openai/hosting.json`.
- All sections are anchors within this single document: `#start`, `#profil`, `#projekte`, `#kontakt`.
- The CV and OnePager are downloadable PDFs under `dist/downloads/`.
