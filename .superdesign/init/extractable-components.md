# Extractable components

None. This is one static HTML page with inline header, footer, hero, and sections; no shared component source is reused across multiple routes. Skip Superdesign component extraction.
